You can use > alert(<string>)

A comment in javascript is > //anything here is a comment

//Basic data types
Integer 10
Float 10.0
Javascript treats them all the same

//Strings
"Hello World"
"10"

// Booleans -- Note: lowercase
true
false

//undefined and null -- similar to None in python?
undefined
null

4 * 4
4 % 4

"Django is cool"
"Django" + "is super cool"

"django".length
"hello \n start a new line"

"hello"[4] = "o"

// VARIABLES -- var varName = value;
var countRuns = 0;
var bankAccount = 100;
var deposit = 50;
var totalBalance = bankAccount + deposit

var greeting = "Welcome back: ";
var name = "Dan";

alert(greeting+name)

console.log(<string>)

prompt("<String> <Question>")

var age = prompt("<something>")

// Operators
<=
>=
<
>
2 == 2
"2" == 2 //True
"2" === 2 // False
5 !== "5" // True
5 != "5" //False

true == 1; //True
true === 1; //False

null == undefined //true
null === undefined //false

// Logical Operators
//AND
1==1 && 2 == 2; // true

//OR
1 == 2 || 1 == 1; // true

//NOT
!(1==1) // false

// Control Flow
IF
if (condition){
    // execute code
}

IF ELSE
if (condition){
    execute
}else{
    execute
    }

IF, ELSE IF, ELSE
if (condition one){
    execute
} else if (condition two){
    execute
}else{
    execute
    }

// WHILE LOOPS
while (condition){
    execute code
    while condition is true
    }

while (condition){
    execute code
    if (another condition){
        break;
        }

// FOR LOOPS
for (statement1; statement2; statement3){
    execute code
    }

    statement 1 is executed before the loop starts
    2 defines the condition for running the loop
    3 is executed each time after the loop cycles through